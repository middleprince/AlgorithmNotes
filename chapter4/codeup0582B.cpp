#include <cstdio>

int main(void)
{
    int iterm, n;
    int num[110], group[110][110];
    if(scanf("%d", &item) == EOF || iterm <1)
        return -1;
    while(iterm--) 
    {
        scanf("%d", &n); 
        for(int i=0; i<n; i++)
            scanf("%d", &num[i]); 
        int k, ;
        for(int j=0; j<n; j++)
        {
            scanf("%d", &k);
            group[k][num[i]]++;
        }

        
    }
    return 0;
}
